export const environment = {
  production: false,
  apiBaseUrl: '/api',
  /** Mock backend is an HTTP interceptor by default; flip to false once json-server or the real NestJS API is reachable. */
  useMockInterceptor: true,
};
